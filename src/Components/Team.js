//import users from '../users.json'
import User from './User'
const Team = ({team}) => {
    return (
        <div className='task'>
            <h3>{team.name}</h3>
        </div>
    )
}
export default Team
